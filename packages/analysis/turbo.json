{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "outputs": ["../../analysis/dist/**"],
      "passThroughEnv": ["GITHUB_WORKSPACE", "PATH"]
    }
  }
}
